<!-- markdownlint-disable MD041 MD024 -->

# Mall Curation Log

Append-only log of curation decisions, judgment calls, and falsifier-trigger evidence for the Plugin Mall.

Established 2026-06-23 per ADR-008 § Falsification: "Track triggers in `docs/ledgers/curation-log.md` tagged `[MALL-AUTOMATION]` and in Mall's own `docs/curation-log.md` tagged equivalently."

## What goes here

- **Editorial decisions** the Mall self-curation surfaces for judgment (a third-party store earns score 0 because the maintainer changed license to NOASSERTION; a new third-party plugin appears as an alternative to a Mall-curated entry; a Mall-curated plugin's `adapted_from` upstream is archived).
- **Falsifier-trigger evidence** for ADR-008 § Falsification (11 triggers tracked through 2026-08-29 retrospective).
- **Convention/policy changes** that affect catalog behaviour (trust scoring weight adjustments, source registry policy shifts).

## What does NOT go here

- **Routine catalog refreshes** (auto-shipped via weekly cron; git log is the audit trail; tagging each refresh here would be noise).
- **Heir-side operational issues** (those go to `Alex_ACT_Memory/feedback/`).
- **Supervisor-side Edition-promotion decisions** (those live in `Alex_ACT_Supervisor/docs/ledgers/brain-qa-changelog.md`).

## Entry format

```markdown
| Date | Tag | Source / trigger | Decision | Evidence |
| --- | --- | --- | --- | --- |
| YYYY-MM-DD | [TAG] | what surfaced the item | M (mechanical) or S (semantic) — what was decided | links / commit SHAs |
```

Tag vocabulary:

- `[MALL-AUTOMATION]` — ADR-008 falsifier-trigger evidence
- `[STORE-LICENSE]` — license-clarity decisions (e.g. NOASSERTION fallback)
- `[STORE-DEMOTION]` — store pruned due to staleness / abandonment / quality drop
- `[PLUGIN-PROMOTION]` — Mall-curated plugin proposed for Edition baseline
- `[TRUST-TUNE]` — trust scoring formula adjustment
- `[CONVENTION]` — schema / format / policy change

## Log

| Date | Tag | Source / trigger | Decision | Evidence |
| --- | --- | --- | --- | --- |
| 2026-08-01 | `[CONVENTION]` | Fabio requested canonical scripts for Mall administration, vendoring, maintenance, and contributor plugin publication with explicit approval | S - adopt one shared packager with dry-run-first admin and contributor wrappers. Contributors can prepare and validate PR payloads but cannot commit, push, merge, or publish through the scripts. The validation check runs on every PR. Plugin, script, marketplace, and source-registry paths are CODEOWNED and require `@fabioc-aloha` approval; generated catalog refresh paths remain zero-review auto-merges after the required check. Add a dry-run-first admin command because CODEOWNERS alone only requests review. | 42/42 tests; Core v0.4.2 remote vendor dry-run PASS; contributor dry-run PASS; canonical admin apply PASS at 96 files with existing Mall metadata preserved and 73 audited link rewrites; live submission validation PASS; curated maintenance PASS (363 curated / 3,848 catalog / 42 stores). Branch protection was absent when checked; apply remains an explicit admin action. |
| 2026-07-28 | `[CONVENTION]` | Approved in-place CLI-native migration under Steward ADR-014 and publication boundary under ADR-015 | S - release Mall 3.0.0 with 365 strict first-party plugin entries. Keep the 49-store, 3,912-plugin external catalog unchanged; keep `v2.0.0` as rollback anchor; treat installation as opt-in and Edition 3.x/4.1 as outside the compatibility claim. Severity: `[constitutional]`. | Feature branch `80a7ab6`; Edition v4.2.0 `c475147`; merged-tree checks 34/34; marketplace assertion 365 names, 365 sources, 0 missing, 0 oversized. |
| 2026-07-01 | `[STORE-DEMOTION]` | Duplicate-only store audit after local catalog refresh: `remotion-skills` (1/1 duplicate) and `obra-superpowers` (14/14 duplicate) | S — **prune both from `sources/supported-stores.json`**. User approved pruning duplicate-only stores. `remotion-skills` only duplicated `remotion`; `obra-superpowers` is canonical upstream but every plugin name is already represented elsewhere, including higher-trust first-party or aggregator entries. | Local duplicate audit against `catalog/index.json` generated 2026-07-01 (`remotion-skills`: 0 unique / 1 duplicate; `obra-superpowers`: 0 unique / 14 duplicate). |
| 2026-06-23 | `[CONVENTION]` | Mall audit surfaced absent `docs/curation-log.md` (ADR-008 § Falsification expected it) | M — create the artifact + define entry format. Zero curation decisions to backfill (Mall has been operational 25 days; no judgment calls surfaced; all 7 catalog refreshes were clean automated runs per `git log -- catalog/`). | Mall audit 2026-06-23; ADR-008 § Falsification |
| 2026-06-23 | `[STORE-LICENSE]` | Tier-1 candidate `ComposioHQ/awesome-claude-skills` — 826 SKILL.md units, 65K stars, active | S — **defer**. GitHub API reports no SPDX license (`license` field empty). Store-evaluation L-dimension scores 0/2 ("No license, custom restrictive license, or unclear terms"); total drops below threshold. Per `/add-source` Boundaries: "Do not skip the scorecard. Stores below score 7 do not get added even if the requester insists." Re-evaluate when upstream adds LICENSE; consider opening upstream issue. | Live `gh api /repos/ComposioHQ/awesome-claude-skills` 2026-06-23 |
| 2026-06-23 | `[STORE-DEMOTION]` | Tier-1 candidate `PatrickJS/awesome-cursorrules` — 40K stars, 253 `.mdc` files at `rules/<name>.mdc`, MIT, very active | S — **defer**. `scripts/scan-sources.cjs listPluginCandidates` walks directories only (`if (!d.isDirectory()) continue;` line 213); `.mdc` files at flat layout are skipped entirely. Catalog Fit is high (entire Cursor ecosystem, zero coverage today) but Scanner Compatibility = 0. Needs scanner extension to recognize file-based catalog units before this store can be ingested. Track as scanner roadmap item. | Mall audit 2026-06-23; scan-sources.cjs:212-216 |
| 2026-06-23 | `[STORE-DEMOTION]` | Tier-1 candidate `microsoft/power-platform-skills` — 380 stars, 54 SKILL.md units at `plugins/<area>/skills/<name>/SKILL.md` (multi-area: code-apps, power-pages, ...), MIT, active | S — **defer**. Scanner walks 2 depth levels (`if (depth > 2) return` line 200); the 3-level layout means `<area>` dirs get cataloged as super-plugins (each has `skills/` marker), masking the actual 54 individual skills. Two paths forward: (a) scanner gains `extraDepth` field per registry entry, (b) one registry entry per area with `pluginDir: "plugins/<area>/skills"` (ongoing maintenance burden — would silently miss new areas). Defer pending scanner extension decision. | Mall audit 2026-06-23; scan-sources.cjs:199-200 |
| 2026-06-23 | `[STORE-LICENSE]` | Tier-2 candidate `trailofbits/claude-code-config` — 2021 stars, security-org credibility, "opinionated defaults + workflows for Claude Code" | S — **defer**. GitHub API reports no SPDX license. Same logic as Composio defer (L=0 disqualifies). Trail of Bits is a respected security firm, so re-evaluate if upstream adds LICENSE (open upstream issue may be appropriate). | Live `gh api /repos/trailofbits/claude-code-config` 2026-06-23 |
| 2026-06-23 | `[STORE-LICENSE]` | Tier-2 candidate `yzhao062/agent-style` — 524 stars, "21 writing rules for AI coding agents (Claude/Codex/Copilot/Cursor/Aider)" | S — **defer**. No SPDX license. Also note: scope overlaps with the Mall's own `humanizer` plugin + Edition's `markdown-author` agent always-on filter (29-pattern de-AI-slop rules); even with license fixed, would need Catalog-Fit re-evaluation for unique value. | Live `gh api /repos/yzhao062/agent-style` 2026-06-23 |
| 2026-06-23 | `[STORE-DEMOTION]` | Tier-2 candidate `instructa/ai-prompts` — 1052 stars, MIT, multi-IDE (Cursor/Cline/Windsurf/Copilot) framework-specific prompts | S — **defer**. `prompts/<framework>/` subdirs (angular-19, appwrite-angular, appwrite-astro, …) contain raw prompt `.md` files without SKILL.md / README.md / plugin.json markers; scanner's `hasPluginMarkers` returns false on each subdir; 0 candidates produced. Same scanner-shape blocker as awesome-cursorrules. Defer pending file-based catalog-unit support. | Mall audit 2026-06-23; scan-sources.cjs:194 |
| 2026-06-23 | `[STORE-DEMOTION]` | Tier-2 candidate `IBM/mcp` — 387 stars, Apache-2.0, "collection of MCP servers, clients and developer tools by IBM" | S — **decline** (not defer). Repo top-level is `CONTRIBUTING.md`, `LICENSE`, `mcp.json`, `README.md` — **no subdirectories at all**. The `mcp.json` is a single-file metadata index pointing to external MCP servers, not a catalog of MCP server units. Catalog Fit = 0 ("Off-topic — no clear use case for an AI agent that consumes plugins from this Mall"). Different from a defer because no upstream change would make this scannable — the repo's purpose is meta-discovery, not catalog hosting. The MCP servers it indexes are already covered by `mcp-servers` (modelcontextprotocol/servers) and `awesome-mcp-servers` (punkpeye). | Mall audit 2026-06-23; bare-tree probe |
| 2026-07-24 | `[PLUGIN-ADDITION]` | New first-party plugin `flint-chart-plugin` (v0.2.0) submitted for `data-analytics/` — two-skill + one-prompt + MCP-sidecar bundle for chart selection and local rendering via `flint-chart-mcp@^0.2.2` (Vega-Lite / ECharts / Chart.js). | S — **accept**. Upstream OSS repo <https://github.com/fabioc-aloha/flint-chart-plugin> is source-of-truth; Mall vendors v0.2.0. Category placement matches nearest existing siblings (`visual-vocabulary`, `chart-interpretation`, `dashboard-design`, `data-visualization`). First Mall plugin with an `mcp` artifact — shape retained as `.SP.` (skill + prompt) since 4-position schema [I][S][P][M=muscle] has no MCP position; `mcp` field added under `artifacts` as schema-first proposal. If Mall extends shape to a 5th position for MCP, bump this plugin then. Dogfooded end-to-end during 2026-07-24 session (AIRS figure reproductions + heart chart demo). | fabioc-aloha/flint-chart-plugin@e7b6cfc; upstream MIT dual-copyright preserving Microsoft attribution on forked flint-chart skill body |
| 2026-07-24 | `[PLUGIN-UPDATE]` | Upstream `fabioc-aloha/flint-chart-plugin` shipped v0.3.0 (commit `88364cf`) with breaking Node prerequisite bump (>=18 to >=22), demo-footprint reduction (3 to 1, keeping only `heart-with-axes`), and a new `Would Revise If` section in `flint-chart` SKILL. | S — **accept and vendor**. Node bump justifies the minor SemVer bump because Node 18/20 installers no longer qualify; Node 18 reached EOL 2025-04-30 and Node 20 goes maintenance-only April 2026. Payload changes: only `skills/flint-chart/SKILL.md` (Would-Revise-If addition) and vendored `README.md` (updated demo narrative + asset src rewrite) — the other three installable files are byte-identical to 0.2.0. `plugin.json` bumped to 0.3.0; `requires_runtime.node` bumped to >=22; `token_cost` estimated at 16650 (weekly catalog refresh will recompute). Demos removed from repo don't affect Mall vendoring (demos/ was already in the `What NOT to include` list). | fabioc-aloha/flint-chart-plugin@88364cf; upstream CHANGELOG 0.3.0 entry |
| 2026-07-25 | `[PLUGIN-UPDATE]` | Upstream `fabioc-aloha/flint-chart-plugin` shipped v0.3.1 (commits `324a4f4`, `da5209b`) fixing a silent install failure: the plugin told adopters to merge its MCP fragment into a workspace-root `.mcp.json`, which VS Code never reads. Surfaced by a real install attempt that produced no tools and no error. | S — **accept and vendor**, and correct the Mall's own `plugin.json`, which carried the identical defect (`install_paths.mcp.merge_target: "workspace-root .mcp.json"`) and was therefore shipping the bug to every heir. VS Code's workspace path is `.vscode/mcp.json`; root `.mcp.json` is the Claude Code convention. The `servers` schema is identical and no error is surfaced, because VS Code isn't parsing a broken file — it reads no file at all. Payload changes: `skills/flint-chart/SKILL.md` (per-host path table + server-vs-client triage ladder), `mcp.json` (explicit `"type": "stdio"`), vendored `README.md`; the other two installable files are byte-identical to 0.3.0. `plugin.json`: version 0.3.1, `merge_target` corrected, `server_config.type` added, `install_notes` added, `token_cost` scaled 16650 → 16959 (payload grew 1.9%; weekly refresh may recompute). Vendored-README rewrite extended beyond images to **all** relative links — 10 were resolving into the Mall folder and 404ing, 7 of them pre-existing since the first publish. `settings.json` deliberately **not** vendored: it registers the `local/` discovery roots, which ACT Edition heirs (`requires_edition >=3.0.0`) already provide; it is only needed on a plain VS Code workspace. | fabioc-aloha/flint-chart-plugin@da5209b; upstream CHANGELOG 0.3.1; end-to-end verified 2026-07-25 against `flint-chart-mcp` 0.2.2 (5 tools, 34 Vega-Lite chart types, validate + render + interactive view) |
| 2026-07-25 | `[PLUGIN-UPDATE]` | Upstream `fabioc-aloha/flint-chart-plugin` shipped v0.3.2, acting on four review findings raised by an adopting workspace that installed 0.3.1 via GitHub Copilot CLI. | S — **accept and vendor**. The sharpest finding extends the plugin's own thesis one host further: GitHub Copilot CLI reads `~/.copilot/mcp-config.json` with top-level key **`mcpServers`**, not `servers` — so it fails *twice over* (wrong path AND wrong schema) where 0.3.1 fixed only the wrong-path case, and just as silently. Verified against a live CLI config and GitHub docs. Also fixed: the README told VS Code users to copy `mcp.json` "as-is", which would silently destroy an existing file's other servers — the `settings.json` asset already carried an additive-merge caution, the MCP asset did not. Added `scripts/verify-install.mjs` (not vendored; repo tooling), making check 1 of the verification ladder executable rather than "ask the agent to probe" — the one check that must not depend on the agent, since the agent may be what is broken. Plus a PowerShell install variant for the Windows-heavy heir population. `plugin.json`: version 0.3.2, `token_cost` scaled 16959 → 17231 (payload +1.6%). | fabioc-aloha/flint-chart-plugin@7b2e952; upstream CHANGELOG 0.3.2 |
| 2026-07-25 | `[CONVENTION]` | Vendored-README link rewriting, and the version-pin policy for `flint-chart-mcp`. | M — record two conventions surfaced while publishing 0.3.2. **(1)** The vendored-README rewrite now covers *all* relative links, not just `assets/` images: the Mall vendors five files, so every relative reference resolves inside the plugin folder and 404s. Ten links were broken on the plugin page, seven of them since the first publish. Upstream's runbook (`docs/publishing-to-mall.md`) was updated to match. **(2)** The `flint-chart-mcp` pin is held at `^0.2.2` **by decision, not lag**. Public npm `latest` is 0.4.0, but it is unreachable from Microsoft corporate npm mirrors (`packagefeedproxy.microsoft.io`), which stop at 0.2.2 and return `ETARGET` even with `--prefer-online`. Since most heirs are corpnet repos, a plain `^0.4.0` would break more adopters than it helps. Reopening conditions are recorded upstream: mirror sync (preferred), or verification that 0.4.0 is a strict superset, in which case the tested dual range `^0.2.2\|\|^0.4.0` resolves per-registry. | fabioc-aloha/flint-chart-plugin@7b2e952 CHANGELOG Unreleased; corpnet mirror measured 2026-07-25 |
| 2026-07-25 | `[PLUGIN-UPDATE]` | Upstream `fabioc-aloha/flint-chart-plugin` shipped **0.4.0, 0.5.0 and 0.5.1** in one session. The Mall vendors 0.5.1 directly, skipping the two intermediates — neither was ever published here, so no heir saw them. The substance is a third skill (`render-verify`), an optional second MCP server, and an upstream wiring fix. | S — **accept and vendor**. The plugin could render a chart but had no way to check the chart said what it was meant to say: a collapsed scale, a merged color scale, or an empty data binding renders as a valid image telling the wrong story, and `validate_chart` cannot catch it. `render-verify` supplies a nine-row chart failure catalog plus an eight-row general catalog (404'd resources, unstyled content, clipped text, font substitution, layout collapse, below-the-fold content, stale render, surviving placeholders), a three-rung capability ladder, and a console-errors-before-picture ordering. Shipped at 0.4.0 as `chart-verify`, renamed `render-verify` at 0.5.0 within the hour because the old name implied chart-only scope — done **before** any Mall publish, so the rename cost 35 in-repo edits instead of breaking every heir install. 0.5.1 then closed an upstream wiring gap: `flint-chart` §0.1 had never handed off to `chart-big-idea`, so an *ambient* request ("chart this data", no slash command) skipped framing entirely — `/render-chart` was the only path that produced a Brief. Also added an intent check that asks whether the artifact should exist at all and whether the stated purpose is the real one. **New optional `playwright` MCP server**, pinned exactly at `0.0.78` (caret on `0.0.x` protects nothing). Measured, not assumed: no bundled browser — Playwright drives an *installed* one by channel, so `--browser msedge` is shipped because Edge ships with Windows where most heirs are and the upstream default (`chrome`) is frequently absent; `file://` navigation is blocked by default, hence `--allow-unrestricted-file-access`, which is documented as unsafe combined with untrusted web browsing; the MCP handshake reports the Playwright *library* version, not the package version. VS Code's built-in browser tools satisfy the whole capability with zero configuration, so **VS Code heirs should omit the server** — GitHub Copilot CLI, a terminal agent with no browser, is the real audience. `plugin.json`: version 0.5.1, third skill added, `install_paths.mcp` reshaped from a single server to a `servers` array with per-server `required` flags, `requires_runtime.browser` added, `token_cost` scaled 17231 → 24513 (payload +42%). **Caveat recorded:** Copilot CLI is the stated primary audience for the optional server but has not been measured — the claim rests on the inference that a terminal agent has no browser. | fabioc-aloha/flint-chart-plugin@5a49812; upstream CHANGELOG 0.4.0 / 0.5.0 / 0.5.1; Playwright behaviour measured 2026-07-25 against `@playwright/mcp@0.0.78` and this repo's `demos/heart-with-axes/report.html` |
| 2026-07-30 | `[PLUGIN-REMOVAL]` | Steward-side editorial judgment: four Mall-curated plugins now redundant with Alex_ACT_Illustrator_Plugin (Steward-maintained plugin at `plugins/data-analytics/flint-chart-plugin/` in this Mall; renamed 2026-07-29 from `flint-chart-plugin`). Illustrator now ships eight skills across four features (Flint / Print figures / Replicate / Shell). | S -- **remove** all four: (1) `data-analytics/visual-vocabulary/` -- literal absorption on 2026-07-30 into illustrator's `chart-vocabulary` skill (Plugin commit 74d40f8); feature parity confirmed in the absorption. (2) `data-analytics/data-visualization/` -- `Story-intent chart selection, color theory, annotation patterns, decluttering rules, title=insight` overlaps illustrator's `chart-big-idea` + `chart-vocabulary` + `flint-chart` \u00a70.2 framing framework; illustrator's approach is more integrated (adds rendering + verification via `render-verify` Prose-coupling). (3) `media-graphics/svg-dashboard-composition/` -- `Compose one self-contained SVG from multiple visual fragments` overlaps illustrator's `print-svg-style-guide` (canvas grammar + composition idioms) + `figure-generator` (deterministic `.mjs` + dataset provenance + print-legibility floor math). (4) `image-generation/imagen/` -- Google Vertex AI direct access to Imagen; illustrator's `replicate-imagery` skill routes to Imagen 4 via Replicate's HTTP API. Per Fabio: intent is `illustrator replaces AI image plugins regardless of provider`. Marketplace.json regenerated: 365 -> 361 entries. Aggregated catalog: 3850 -> 3846. All `npm run check` gates pass (34/34 tests, catalog validate PASS). | Steward brain-qa-changelog to follow; illustrator plugin commit references in `chart-vocabulary` absorption 74d40f8, `replicate-imagery` activation  a231b4, DDA Phase 2 (`print-svg-style-guide` + `figure-generator`)  6a59d9. Not tagged as `[PLUGIN-DEMOTION]` because these were not demoted for quality reasons; they were superseded by a more integrated plugin. |
| 2026-07-30 (PM, night IV) | plugins/data-analytics/ | [PLUGIN-UPDATE] Renamed and expanded `flint-chart-plugin` → `alex-act-illustrator-plugin` in place. The plugin renamed upstream on 2026-07-29 and expanded from 3 skills to 8. Deleted old folder `plugins/data-analytics/flint-chart-plugin/` (4 files: plugin.json, .mall-metadata.json, README.md, skills/{chart-big-idea,flint-chart,render-verify}/SKILL.md, commands/render-chart.md). Created new folder `plugins/data-analytics/alex-act-illustrator-plugin/` with 15 files: plugin.json (3 mcpServers: flint required, replicate + playwright optional), .mall-metadata.json (11 artifacts, tier=standard, token_cost=48000, requires_edition>=3.0.0, upstream=Alex_ACT_Illustrator_Plugin), README.md (Mall-side), commands/render-chart.md, and 8 skill folders (chart-big-idea + chart-vocabulary + docs-shell + docs-shell/starter/{index.html,manifest.json,about.md} + figure-generator + flint-chart + print-svg-style-guide + render-verify + replicate-imagery). Surgical catalog edit on catalog/stores/plugin-mall.json preserved trust_score=95 and trust_signals (first-party provenance stable). `render-catalog.cjs` + `render-marketplace.cjs` regenerated downstream artifacts. Validation: `npm run check` PASS. Category kept as `data-analytics` per Fabio decision (discoverability continuity). Marketplace: 361 entries (unchanged — 1:1 rename). Aggregated catalog: 3846 plugins (unchanged from post-removal state at commit `e739dd6`). |
